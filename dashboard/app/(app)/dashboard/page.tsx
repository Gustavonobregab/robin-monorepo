/* Placeholder — UI removed for a clean rebuild. The v1 page (with all its
   logic: data fetching, handlers, job flow, error handling) is at git HEAD:
     git show 'HEAD:dashboard/app/(app)/dashboard/page.tsx'
   Rebuild the new UI here, reusing that logic. */
export default function DashboardPage() {
  return (
    <div className="py-8">
      <h1 className="text-2xl font-medium tracking-tight">Dashboard</h1>
      <p className="mt-2 text-sm text-neutral-500">This screen is being rebuilt.</p>
    </div>
  )
}
